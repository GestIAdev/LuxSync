feat(selene): WAVE 7556-7557 — YIN YANG: ambient/transit effects go proactive

The Dream Simulator only predicted high-energy events (drop_incoming,
buildup_starting) and its zone override only scaled UP (peak/intense).
Ambient/transit effects (latin_bubbles, etc.) were never simulated
proactively — they only fired as Sovereign Clock reroute consolation
when a heavy/divine effect was aborted.

WAVE 7556 — YIN YANG (bidirectional prediction):
- Bug 1+2: Zone override now also scales DOWN. breakdown_imminent maps
  to gentle, energy_drop maps to ambient. The groove cap only applies
  to ascending overrides; descending ones are uncapped to open the pool.
- Bug 3: filterByPressure relaxGuardsForFuture is now bidirectional.
  For drops/buildups it relaxes the MIN gate (pressure will rise).
  For breakdowns/valleys it relaxes the MAX gate (pressure will fall).
- Fix 4: Scoring bonus (+0.15) for archetype ambient/utility effects
  with aggression <= 0.60 during breakdown/energy_drop predictions.
  Symmetric to the IMPACT_EFFECTS boost that heavy candidates receive
  during drops. Added archetype field to RegistryEntry (propagated
  from CognitiveDNA in _buildEntryFromV3).

WAVE 7557 — Organic Kinematic Confidence:
- Exterminated the hardcoded probability 0.50 magic number in the
  energy_drop heuristic fallback (PredictionEngine.ts). Replaced with
  an organic confidence derived from the energy descent velocity:
    dropVelocity = abs(velocity)  (already computed, was unused)
    normalizedDrop = clamp(dropVelocity / 0.05, 0, 1)
    organicProbability = 0.45 + (normalizedDrop * 0.45)  range 0.45 to 0.90
  Gentle drift (v near 0) gives 0.495 (below 0.55 threshold, no
  proactive buffering — correct, not a real breakdown). Steep cliff
  (v >= 0.05) gives 0.90 (well above threshold, full guard relaxation).
  ETA also scales inversely with velocity (8 beats to 2 beats).
- Reverted the WAVE 7556.1 temporary patch (threshold 0.40 for
  low-energy). Universal threshold 0.55 restored — energy_drop now
  crosses it naturally when the descent is real.

tsc --noEmit green (only pre-existing hyperion-render.worker.ts error).

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
