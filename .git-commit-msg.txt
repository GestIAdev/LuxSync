fix(selene): WAVE 7560.1 — lower minority relaxation threshold to 0.25

Field logs showed breakdown_imminent with conf=0.386 and conf=0.261
failing to relax pressure guards (threshold was 0.40). The architect's
reasoning: breakdowns are statistical minorities, so even 0.40 raw
confidence is a loud signal. But computeOrganicConfidence applies PLL
and syncopation factors that measure TEMPORAL precision, not structural
knowledge — these can drag a legitimate breakdown prediction from 0.33
Markov mass down to 0.26.

The threshold is now 0.25 for minority events, aligned with the WAVE
7559 Markov posterior threshold (P(breakdown) > 0.25). If the Markov
says breakdown is significant enough to predict, that same signal is
significant enough to relax the pressure guard. The PLL doesn't need
to be perfect for the structure to be trusted.

Majority events (drop, buildup, spike) remain at 0.55.

tsc --noEmit green (only pre-existing hyperion-render.worker.ts error).

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
