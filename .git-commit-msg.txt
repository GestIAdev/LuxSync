fix(selene): WAVE 7557.1-7558 — kinematic velocity sign + structural breakdown bonus

WAVE 7557.1 — Kinematic velocity sign fix:
- The organic kinematic confidence (WAVE 7557) used Math.abs(velocity)
  which gave moderate probability even when energy was RISING (flashbang).
  During a flashbang (valley to intense), smoothedEnergy lags behind
  rawEnergy, so predictFromEnergy saw trend=falling + currentEnergy<0.5
  and fired energy_drop with moderate confidence — a false positive.
- Fix: use Math.max(0, -velocity) instead. If velocity > 0 (rising),
  dropVelocity = 0, probability = 0.45 (below 0.55 threshold, no guard
  relaxation). Only actual descents produce meaningful confidence.

WAVE 7558 — Structural Breakdown Bonus:
- Root cause analysis of field log (buscandoambient.md): breakdown_imminent
  from the Markov chain NEVER appeared in predictions. The energy
  heuristic's buildup_starting (prob 0.55-0.75) always beat the Markov
  chain's breakdown_imminent (prob ~0.40-0.50) in predictCombined
  arbitration. The YIN YANG zone override never triggered for low-energy
  events, so ambient/transit effects were never simulated proactively.
- The energy heuristic can detect rising energy (velocity>0) but CANNOT
  detect structural breakdowns — those require song structure knowledge
  that only the Markov chain has. This is the symmetric gap to the
  spectral buildup boost (which helps high-energy predictions).
- Fix: +0.15 structural bonus for breakdown_imminent in predictCombined
  when the energy heuristic predicts something else. Lets breakdown_imminent
  (0.45) compete with buildup_starting (0.55), but not override a strong
  buildup (0.70+). Capped at 0.85. Only applies when energy heuristic
  agrees it's NOT already a low-energy event (no double-counting).

Field log confirmed: latin_bubbles only fired via Sovereign Clock reroute
(downgrade from heavy effects), never proactively. With this fix,
breakdown_imminent should surface in predictions and trigger the YIN YANG
zone override (peak/intense to gentle/ambient) proactively.

tsc --noEmit green (only pre-existing hyperion-render.worker.ts error).

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
