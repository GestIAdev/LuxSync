WAVE 7605: SCORCHED EARTH — Single fixture path, .json only, no factory

Architect-directed unification of the fixture library system:
- Eliminated factoryLibPath/customLibPath pathfinder (3 candidates → 1 path)
- Removed all factory seeding logic (no more first-boot copy of .fxt files)
- Removed librerias/ from extraResources (ships with ZERO factory fixtures)
- scanFixtureFolderRecursive: .json ONLY, .fxt parsing reserved for OFL import tool
- rescanAllLibraries(): single recursive scan, no factory+custom merge
- initializeVaults(): only creates userData/fixtures/ (no factory/custom subfolders)
- All IPC handlers (list-all, save-user, delete-user, save/delete-definition)
  now use getFixturesPath() = userData/fixtures/ — dev = prod, no exceptions
- Backward-compatible: systemFixtures:[] in response, frontend merges normally
- Fix: ForgeView index.tsx null-safe physics.motorType access (post-save crash)

Result: 1 path, 1 format, 0 factory fixtures, dev = prod.

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
