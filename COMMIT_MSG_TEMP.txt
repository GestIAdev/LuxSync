WAVE 7749.102: WNS soft gate + Flux 0.20 + hi-hat exclusion — kill synth FPs in silence

User reported 6 synth FP onsets in a silent passage (no kick, no snare,
just "synthes graves"). Analysis showed 3 key discriminators vs real
snares from missedsnare.md:

  gH:   synth 0.214-1.000 (ALIVE) vs real 0.026-0.029 (DEAD)
  WNS:  synth 0.000-0.192 vs real 0.220-1.000
  Flux: synth 0.070-0.310 vs real 0.311-0.475

Three fixes targeting different FP profiles:

1. WNS SOFT GATE ON CRACK PATH — when gH > 0.1 AND WNS < 0.05, reduce
   crackDrive by 70%. Synth bass stabs have energy in the snare band
   (high SnareE → high sEF, high bFct) but no white noise (WNS=0).
   Real snares have noise from snare wires. Dead-gate real snares
   (gH≈0) are unaffected.
   Blocks onsets 1, 3, 5 (WNS=0, gH=0.2-1.0, crackD→0.03-0.05 < fFloor)

2. RAISED BYPASS FLUX 0.15→0.20 — onset 4 had Flux=0.151 and slipped
   through the bypass rescue. All missedsnare real snares have
   Flux > 0.25, so 0.20 is safe.
   Blocks onset 4 (Flux=0.151 < 0.20)

3. HI-HAT EXCLUSION IN BYPASS RESCUE — if SnareE < 0.2 AND hE > 0.5,
   the bypass rescue is catching a hi-hat/cymbal/noise burst, not a
   snare. Onset 6 had SnareE=0.111, hE=0.662 (hi-hat signature).
   missedsnare real snares had hE 0.29-0.49 (all < 0.5).
   Blocks onset 6 (SnareE=0.111, hE=0.662)

Result: 5 of 6 synth FPs blocked. Onset 2 (WNS=0.192, Flux=0.194,
gH=0.772) still fires — it has genuine white noise and moderate flux,
making it indistinguishable from a real snare without alive-gate
reference data.

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
