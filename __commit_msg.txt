WAVE 7622.2: Fix stale pool entry destroying spatial target in L2 tick

The _overridePool entry was created in a previous tick (possibly classic
mode) and did NOT have targetX/Y/Z — those were written by
applySpatialTarget directly to the arbiter's motor override, not to the
pool. When the L2 engine reused the pool entry in IK mode, it wrote
pan_offset/tilt_offset but NO targetX to the arbiter via
setMotorKineticOverride — destroying the spatial target and dropping
the fixture out of IK mode.

Fix: When reusing the pool entry in IK mode (hasSpatialTarget=true),
merge targetX/Y/Z from existingMotor (the arbiter's current motor
override) into the pool entry before writing it back. Also clean up
stale spatial target keys in classic mode.

Also updated telemetry to log the actual mode (IK/CLASSIC), rec keys,
and the correct output values (pan_base for classic, pan_offset for IK).

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
