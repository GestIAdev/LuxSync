WAVE 7635: Fix soft-clipper — soft limiter replaces tanh compressor

TELEMETRY ANALYSIS (WAVE 7634):

  Fixture top-center: basePan=53, headroom=53, rawDelta=-48, ratio=0.905
  53 - 48 = 5 ≥ 0  →  The signal FITS within the DMX range!

  But tanh(0.905) = 0.719 → safeDelta = -38.1 (21% compression)
  The tanh compressor was compressing signals that didn't need
  compression. tanh starts compressing from ratio=0, not from
  ratio=1. At ratio=0.5, tanh(0.5)=0.462 → 7.6% compression.
  At ratio=0.9, tanh(0.9)=0.716 → 20.4% compression.

  This was the "70% amplitude soft-clip" on the top-center fixture.

FIX: Replace tanh compressor with a hard-knee soft limiter.

  if |delta| <= headroom:
    safeDelta = delta          // LINEAR — no compression, waveform preserved
  else:
    safeDelta = tanh(|delta|/headroom) * headroom * sign(delta)  // smooth limit

  At ratio=0.905 (top-center): absDelta=48 <= headroom=53
  → safeDelta = -48.0 (ZERO compression!)
  At ratio=1.5 (overflow): tanh(1.5)=0.905 → safeDelta = 0.905*headroom
  → smooth approach to boundary, no hard clip

Applied to both 8-bit and 16-bit paths, pan and tilt.
Also removed the WAVE 7634-TELEMETRY diagnostic logs.

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
