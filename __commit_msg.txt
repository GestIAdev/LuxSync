WAVE 7631-DIAG: Add diagnostic logs to trace Amnesia Bug culprit

The save works (logs confirm), but the in-memory position reverts to
the old value after saving. Added diagnostic console.logs to trace
exactly what overwrites the position:

1. updateFixture — logs position edits with caller stack trace
2. syncFixturesFromTruth — logs when it fires with truth positions
3. onLoaded listener — logs when show is reloaded from backend
4. FixtureInspector — logs what position it reads from the store

User should:
1. Edit a fixture's position (e.g. Y from 4 to 6)
2. Click Save
3. Re-select the fixture
4. Check console for the sequence of logs

The logs will reveal which code path overwrites the position back to 4.

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
