WAVE 7669: Phase 3 UI construction — spatial targeting, mode switch, trim D-Pad

3a — Mode Switch & Anti-Whiplash Arbitration:
- Add controlMode state ('spatial' | 'mechanical', default 'mechanical')
- Segmented toggle in Zone A header
- Spatial→Mechanical transition: hydrate pan/tilt from getFixturesState
  THEN dispatch releaseSpatialTarget (clears Spatial Gate, prevents whiplash)

3b — TargetingPanel (new subcomponent):
- Numeric X/Y/Z inputs (0.25m steps) replacing 2D pad + elevation slider
- Presets: Centre / Downstage / Upstage
- Aim Target + Release buttons with status line (aimed/warning/error)
- F2 FIX: payload includes fixturePositions + fixtureIKProfiles from stageStore
  → eliminates spatial amnesia (stale {0,0,0} Orchestrator fallback)
- Preflight checks: block dispatch if !isPlaced, !validPosition, !IK-eligible

3c/3d — Precision Offset Upgrade:
- Integrate OffsetTrimPad (XY drag pad, ±30°, 0.5° snap, double-click reset)
- Build 8-direction Trim D-Pad with center zero
- Trim step selector: 0.5° / 1° / 5° (distinct from aim D-Pad's 1/5/15/45)
- All trim dispatches via updateCalibration → 100ms throttle → updateLiveCalibration

3e — Header & Rack:
- DMX LIVE/OFFLINE pill (useLibraryStore.dmxStatus.connected)
- Dirty dot (session.isDirty) with pulse animation
- Remove hardcoded misleading ⟳ ARMED badge
- Rack IK badges: ✓IK (placed + capable) / ⚠NP (capable but not placed)

3f — Keymap Mode Guard:
- WASD/arrows only drive mechanical aim when controlMode === 'mechanical'
- In spatial mode, pan/tilt are IK-owned — manual overrides would fight Spatial Gate

3g — Cleanup:
- Delete CalibrationTargetMini.tsx (superseded by TargetingPanel)
- Strip dead cal-target-mini* CSS from erebus.css

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
