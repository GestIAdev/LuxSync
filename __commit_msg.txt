WAVE 7617: Silence telemetry + fuse L2 patterns with IK spatial aiming

1. Silenced backend telemetry that was flooding logs at 44Hz:
   - [MATH-INPUT] in NodeResolver._writeNodeIK (commented out)
   - [MATH-OUTPUT] in NodeResolver._writeNodeIK (commented out)
   - [PHYSICS-3D] in PhysicsPostProcessor (commented out)
   All three can be re-enabled with debug flags if needed.

2. L2 Pattern Fusion in NodeResolver._writeNodeIK:
   When a Spatial Target is active, the IK path was completely bypassing
   the L2 pattern output (Sweep, Circle, etc.) from AetherKineticEngine.
   The arbiter writes the pattern's fused pan/tilt to channelValues['pan']
   and channelValues['tilt'], but _writeNodeIK only read targetX/Y/Z and
   pan_offset/tilt_offset (L0 VMM), ignoring the L2 pattern entirely.

   Fix: Extract channelValues['pan']/'tilt'] (the arbiter's fused pattern
   output) and compute the orbital deviation from neutral center (0.5):
     patternDeviation = (patternOutput - 0.5) * 2  → [-1, +1]
   Add this as a DMX delta on top of the IK solution, scaled by amplitude,
   distance scale, and gimbal lock fade factor. This makes Sweep/Circle
   orbit around the 3D target instead of ignoring it.

   The same fusion is applied in both 8-bit and 16-bit domains, with
   the gimbal lock fade applied to the pan deviation based on the
   current tilt position.

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
