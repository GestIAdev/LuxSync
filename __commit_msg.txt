WAVE 7670: Phase 4 legacy migration — reconcile pre-7605 fixture paths

Main-process migrator (LegacyFixtureMigrator.ts):
- One-shot, idempotent reconciliation of pre-WAVE-7605 fixture paths
- Runs in StagePersistence.loadShow AFTER zone normalization, BEFORE
  hydration (orchestrator.setFixtures → FixtureProfileResolver)
- SKIP: fixtures that already resolve or carry inline channels
- DETECT: .fxt extension, librerias/factory/custom folder markers
- MATCH: basename-stem vs library id, then model/name case-insensitive,
  then normalized name (strip _- and whitespace)
- REWRITE on unique match: profileId, definitionPath, channels (inlined),
  channelCount
- NEVER guess on ambiguity (>1 candidate) — log and leave untouched
- Safety: .pre-7605.bak backup created before first migrated persist
- Logs: per-fixture skip/migrate/ambiguous/unmatched + summary line

Wiring (StagePersistence.loadShow):
- Import migrateLegacyFixtures
- Call after zone normalization block, before auto-save
- Create .pre-7605.bak via fsp.copyFile (only if not already present)
- Persist migrated show in a single bulk write
- Idempotent: re-running is a no-op (healthy fixtures skipped by step 1)

UI empty state (CalibrationView DMX Channel Grid):
- Replace generic "No channels" with actionable diagnostic when a fixture
  is selected but has no channels:
  "Profile unresolved — This fixture's definition (<path>) is missing
  from the library. Re-assign a profile in the Forge."
- Falls back to generic message when no fixture selected

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
