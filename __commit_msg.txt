WAVE 7671: Phase 3.5 CSS polish — fix OffsetTrimPad + D-Pad layout collapse

Problem: Phase 3 injected OffsetTrimPad and Trim D-Pad into the offset
config panel, but they stacked vertically with overlapping labels,
squished numeric inputs, and no visual separation.

Fixes:
1. Wrap OffsetTrimPad + Trim D-Pad in .trim-controls-row (flex row,
   gap 1rem, align-items flex-start) — pad sits left, D-pad right
2. .cal-trim-pad-label: switch to flex column so "Offset Trim" title
   and "Pan: 0.0° / Tilt: 0.0°" values stack cleanly with display:block
3. .cal-num-input: explicit width, padding, background, border-radius
   matching LuxSync theme (rgba black 0.35, cyan focus border)
4. .cal-num-field-label: fixed min-width 22px, right-aligned, mono font
5. .trim-dpad: grid 3x3 with 4px gap, fixed 96px width, centered icons
6. .trim-step-selector: stacked vertically (label above buttons),
   buttons flex 1 with hover state
7. Remove duplicate .offset-config .cal-trim-pad-* block at file end

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
