WAVE 7611: Hyperion Spatial Radar unlock

Remove the intentional UI freeze on the SPATIAL radar tab. The calibration
pipeline was fully repaired in WAVE 7610 (16-bit IK, live hot-reload,
node.ikCalibration populated), so the "frozen until calibration" safeguard
is no longer necessary.

- KineticsCathedral.tsx: Remove disabled attribute, frozen class, and
  [CALIBRATION REQ] lock label from the SPATIAL paradigm button
- KineticsCathedral.css: Remove .kc-paradigm-btn--frozen and
  .kc-paradigm-btn__lock styles

The SpatialTargetPad already:
- Reads stage dimensions dynamically from stageStore (adapts to 50x25x15m)
- Maps pad X to world X (-width/2 to +width/2)
- Maps pad Y to world Z (-depth/2 to +depth/2)
- Maps slider to world Y (0 to height)
- Dispatches Target3D via setSpatialTarget to KineticsBridge to Aether IPC

No new wiring needed — the pipeline was already built, just locked.

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
