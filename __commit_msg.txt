WAVE 7668: Phase 2 state migration — live offset feedback via useCalibrationSession

- Relocate useCalibrationSession.ts from erebus/calibration/ to CalibrationView/
  (import path adjusted: ../../../core/stage/ShowFileV2)
- Delete standalone panOffset/tiltOffset/panInvert/tiltInvert useStates
- Delete fragile OFFSET SYNC useEffect (depended on [activeFixtureId, stageFixtures]
  → clobbered in-progress edits on any unrelated fixture mutation)
- R1 FIX: getFixture backed by useRef(stageFixtures) → stable identity →
  hook's fixture-load effect re-runs ONLY on fixtureId change
- Wire offset sliders + polarity toggles to liveCalibration reads +
  updateCalibration writes (throttled 100ms → updateLiveCalibration)
- Rewrite handleSaveOffsets to delegate to hook.apply() (preserves
  idle/saving/saved/error UI feedback wrapper)
- Rewrite handleResetOffsets to delegate to hook.reset()
- Add handleRevertOffsets + REVERT button (net new capability for the Lab)
  — disabled when no snapshot or !isDirty
- Add .action-btn.revert CSS (amber accent)

Net effect: dragging an offset slider now produces live physical head
movement within ~100ms via updateLiveCalibration → resolver mutates
node.ikCalibration in-place → next TickEngine frame uses new offsets.

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
