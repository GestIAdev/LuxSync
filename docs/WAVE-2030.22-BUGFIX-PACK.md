# WAVE 2030.22: BUGFIX PACK 🔧
## Audio Persistence + Curve Editor NaN Fix

**Fecha**: 2026-02-12  
**Estado**: ✅ COMPLETADO  
**Tipo**: HOTFIX

---

## FILOSOFÍA

> *"Los bugs son deuda técnica. Se pagan al contado, no a plazos."*

Dos hotfixes críticos: uno afectaba la experiencia de usuario (sesión que no cargaba audio), el otro afectaba la herramienta de autor (curvas de color no renderizaban). Ambos violaban el Axioma Perfection First.

---

## BUG 1: AUDIO PERSISTENCE FAILURE

### Síntoma:
Al salir de Chronos y volver, el `SessionStore` tenía `audioRealPath` pero el audio NO se cargaba porque el `fetch()` fallaba silenciosamente.

### Root Cause:
En `ChronosLayout.tsx` línea 286, se hacía:
```typescript
fetch(`file:///${session.audioRealPath.replace(/\\/g, '/')}`)
```

**Problema**: Si `session.audioRealPath` es `C:\Users\...`, esto generaba:
```
file:///C:\Users\...  // Los backslashes NO se reemplazan en la template string
```

Y `fetch()` requiere:
```
file:///C:/Users/...  // Forward slashes + prefijo file:///
```

### Fix:
1. Normalizar path ANTES de construir la URL
2. Verificar que no se duplique el prefijo `file://`
3. Reemplazar **todos** los backslashes por forward slashes

```typescript
// ⚒️ WAVE 2030.22: BUGFIX - Normalize audio path for Windows
let normalizedPath = session.audioRealPath.replace(/\\/g, '/')
if (!normalizedPath.startsWith('file://')) {
  normalizedPath = `file:///${normalizedPath}`
}
fetch(normalizedPath)
```

### Validation:
- Windows path: `C:\music\track.mp3` → `file:///C:/music/track.mp3` ✅
- Unix path: `/home/user/track.mp3` → `file:////home/user/track.mp3` (extra slash no daña)
- Already prefixed: `file:///C:/...` → sin duplicado ✅

---

## BUG 2: CURVE EDITOR NaN (Color Curves)

### Síntoma:
Al crear curvas de tipo `color` (valueType: 'color'), el path SVG NO se renderizaba. Coordenadas resultaban en `NaN`.

### Root Cause:
En múltiples lugares del código, se asumía que `keyframe.value` era un `number`:
```typescript
const y0 = toY(kf.value as number)  // ❌ Si es color, value = { h, s, l } → NaN
```

Aunque el `toY()` tenía lógica para manejar objetos HSL, los casts `as number` estaban **antes** de pasarle el valor, causando que se intentara hacer aritmética con objetos.

### Fix:
**Arquitectura**: Crear un helper `getPlotValue()` que extrae el valor numérico plotteable:

```typescript
/**
 * Extract the plottable numeric value from a keyframe value.
 * For color curves: uses hue (0-360) normalized to 0-1.
 * For numeric curves: returns the value directly.
 */
function getPlotValue(
  value: number | { h: number; s: number; l: number }, 
  valueType: 'number' | 'color'
): number {
  if (valueType === 'color' && typeof value === 'object' && 'h' in value) {
    return value.h / 360  // Normalize hue to 0-1 for plotting
  }
  return value as number
}
```

### Cambios:

| Ubicación | Antes | Después |
|-----------|-------|---------|
| `toY()` | `value as number` branching | `getPlotValue(value, curve.valueType)` unified |
| Bezier handle drag (línea 415) | `toY(kf.value as number)` | `toY(kf.value)` |
| Bezier handle drag (línea 424) | `toY(kf.value as number)` | `toY(kf.value)` |
| Bezier handle render (línea 698) | `toY(kf.value as number)` × 2 | `toY(kf.value)` × 2 |

### ¿Por qué esto funciona?
- **Color curves**: `getPlotValue()` devuelve `hue / 360` → valor 0-1 → `toY()` lo mapea al canvas
- **Numeric curves**: `getPlotValue()` devuelve el valor directo → `toY()` aplica range mapping
- **Zero casts**: El tipo union en `toY()` es respetado → TypeScript feliz

---

## ARCHIVOS MODIFICADOS

| Archivo | Cambio |
|---------|--------|
| `ChronosLayout.tsx` | Normalización de `audioRealPath` antes de `fetch()` |
| `CurveEditor.tsx` | +`getPlotValue()` helper, `toY()` refactor, removal de `as number` casts |

---

## VALIDACIÓN

### BUG 1 (Audio Persistence):
1. Cargar audio en Chronos
2. Navegar a Dashboard
3. Volver a Chronos
4. **Antes**: audio no cargaba, consola mostraba fetch error
5. **Después**: audio carga automáticamente desde path ✅

### BUG 2 (Color Curve NaN):
1. En Hephaestus Studio, crear curva de tipo `color`
2. Añadir 2+ keyframes con HSL values
3. **Antes**: path SVG no renderizaba (coordenadas NaN)
4. **Después**: curva renderiza como línea suave basada en `hue` (H de HSL) ✅

---

## LECCIONES

1. **File URLs en Windows**: SIEMPRE normalizar paths a forward slashes antes de construir `file://` URLs
2. **Type casts en coord math**: Si un valor puede ser objeto OR número, crear helpers tipados en vez de `as number` everywhere
3. **SVG debugging**: Si un path no renderiza, revisar si alguna coordenada es `NaN` — SVG ignora todo el path si una coord es inválida

---

**WAVE 2030.22: BUGFIX PACK — SEALED.** 🔧⚒️
