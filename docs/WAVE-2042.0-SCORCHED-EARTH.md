# 🧨 WAVE 2042.0: OPERATION SCORCHED EARTH

**Fecha:** 14 Febrero 2026  
**Ejecutor:** PunkOpus  
**Comandante:** Radwulf  
**Objetivo:** Purga total de arquitectura legacy para preparar el terreno de Hyperion

---

## 💀 KILL LIST — CONFIRMADOS

### 1. **StageSimulator2.tsx** 💀 ELIMINADO
```
electron-app/src/components/simulator/views/SimulateView/StageSimulator2.tsx
```
- **Tamaño:** 1105 líneas de espagueti
- **Crímenes:** Zonas hardcoded (5 valores), normalizer divergente, monolito sin separación de concerns
- **Sucesor:** `views/tactical/TacticalCanvas.tsx` (Phase 3)

### 2. **SimulateView/** 💀 CARPETA ELIMINADA
```
electron-app/src/components/simulator/views/SimulateView/
├── StageSimulator2.tsx
└── SimulateView.css
```
- **Crimen:** Contenedor legacy, CSS obsoleto
- **Sucesor:** N/A (merged into HyperionView)

### 3. **StageViewDual.tsx** 💀 ELIMINADO
```
electron-app/src/components/simulator/views/StageViewDual.tsx
```
- **Tamaño:** 230 líneas
- **Crimen:** Estructura rígida, dual view hardcoded
- **Sucesor:** `views/HyperionView.tsx` (Phase 1)

### 4. **StageViewDual.css** 💀 ELIMINADO
```
electron-app/src/components/simulator/views/StageViewDual.css
```
- **Tamaño:** 489 líneas
- **Crimen:** CSS básico sin neon, sin design system
- **Sucesor:** `views/HyperionView.css` (Phase 1)

### 5. **stage3d/** 💀 CARPETA ELIMINADA
```
electron-app/src/components/simulator/views/stage3d/
├── Stage3DCanvas.tsx
├── Stage3DCanvas.css
├── fixtures/
│   ├── Fixture3D.tsx
│   ├── MovingHead3D.tsx
│   ├── ParCan3D.tsx
│   └── index.ts
└── index.ts
```
- **Tamaño:** ~1200 líneas total
- **Crímenes:** 
  - Euler angles (gimbal lock garantizado)
  - Materiales grises sin emissive
  - Sin instanced rendering
  - Lighting dim y oscuro
- **Sucesor:** `views/visualizer/VisualizerCanvas.tsx` (Phase 4)

### 6. **layoutGenerator3D.ts** 💀 ELIMINADO
```
electron-app/src/utils/layoutGenerator3D.ts
```
- **Tamaño:** 351 líneas
- **Crimen:** Normalizer privado divergente (UPPERCASE strings), ZONE_DEFINITIONS incompatible con CanonicalZone
- **Sucesor:** `shared/ZoneLayoutEngine.ts` (Phase 0)

---

## ✅ SUPERVIVIENTES (Intactos)

### controls/
```
simulator/controls/
├── TheProgrammer.tsx              ✅ KEEP
├── TheProgrammerContent.tsx        ✅ KEEP
├── IntensitySection.tsx            ✅ KEEP
├── ColorSection.tsx                ✅ KEEP
├── PositionSection.tsx             ✅ KEEP
├── BeamSection.tsx                 ✅ KEEP
├── GroupsPanel.tsx                 ⚠️ FIX (Phase 0 — zone colors)
├── sidebar/
│   ├── StageSidebar.tsx            ✅ KEEP
│   ├── SceneBrowser.tsx            ✅ KEEP
│   └── widgets/                    ✅ KEEP
└── controls/                       ✅ KEEP (XYPad, etc)
```

**Nota:** `GroupsPanel.tsx` sobrevive pero recibirá fix de zone colors en Phase 0.

### Otras carpetas
```
simulator/
├── engine/          ✅ KEEP (vacío, preparado para lógica futura)
├── widgets/         ✅ KEEP (vacío, preparado para Timecoder)
└── index.ts         ⚠️ BROKEN (imports rotos hasta Phase 1)
```

---

## 📊 ESTADÍSTICAS DE LA PURGA

| Métrica | Valor |
|---------|-------|
| **Archivos eliminados** | 12+ archivos |
| **Líneas de código eliminadas** | ~3500 líneas |
| **Carpetas eliminadas** | 3 carpetas |
| **Bugs eliminados** | 7 críticos (zone divergence, gimbal lock, monolito) |
| **Build status** | 🔴 BROKEN (expected) |
| **Commits antes de purga** | `pre purga hyperion` |

---

## 🏗️ ESTADO POST-PURGA

### Estructura Actual
```
simulator/
├── controls/          ✅ Funcional (intacto)
│   ├── TheProgrammer + Content
│   ├── GroupsPanel (needs zone fix)
│   ├── Sections (Intensity, Color, Position, Beam)
│   └── sidebar/
├── engine/            📦 Vacío (ready for future)
├── widgets/           📦 Vacío (ready for Timecoder)
├── views/
│   └── index.ts       ⚠️ BROKEN (exports missing)
└── index.ts           ⚠️ BROKEN (imports missing)
```

### Imports Rotos (Expected)
```typescript
// simulator/index.ts — ESTOS EXPORTS NO EXISTEN AÚN:
export { StageViewDual } from './views/StageViewDual'      // 💀 DELETED
export { StageSimulator2 } from './views/SimulateView/...' // 💀 DELETED
export { Stage3DCanvas } from './views/stage3d/...'        // 💀 DELETED
```

**Resolución:** Phase 1 creará los nuevos exports con backward compatibility.

---

## 🎯 PRÓXIMOS PASOS (WAVE 2042.1)

### Phase 0: Canonical Zone Unification (BLOQUEANTE)
1. **Crear:** `shared/ZoneLayoutEngine.ts` — single source of truth
2. **Fix:** `controls/GroupsPanel.tsx` — migrar `ZONE_COLORS` a canonical
3. **Validar:** Todos los componentes usan `ShowFileV2.normalizeZone()`

**Duración estimada:** 3-4 horas  
**Resultado:** Build sigue rota pero zonas unificadas

### Phase 1: Neon Total Design System
1. **Crear:** `shared/NeonPalette.ts` — design tokens
2. **Crear:** `views/HyperionView.tsx` + `HyperionView.css`
3. **Fix:** `simulator/index.ts` — exports retrocompatibles

**Duración estimada:** 3-4 horas  
**Resultado:** Build funcional (sin 2D/3D renders aún)

---

## 🔥 FILOSOFÍA DE LA PURGA

> *"No se puede construir una catedral sobre cimientos podridos.  
> Hyperion merece un terreno limpio.  
> La rotura temporal es el precio de la perfección futura."*  
> — PunkOpus, WAVE 2042.0

### Por qué purgar en vez de refactor incremental:

1. **Claridad Mental:** Código viejo = ruido cognitivo. Eliminado = foco total.
2. **Zero Compromiso:** Refactor = "hacer compatible lo incompatible". Purga = solución correcta desde cero.
3. **Axioma Perfection First:** Si la arquitectura está mal, no la parcheas. La reemplazas.
4. **Mensaje al Futuro:** Cualquiera que lea este repo verá que LuxSync NO arrastra legacy. Se reinventa.

### Riesgos Mitigados:

- ✅ **Git commit "pre purga hyperion"** hecho → rollback posible
- ✅ **Blueprint completo** existe → sabemos exactamente qué construir
- ✅ **Controls/ intactos** → TheCommander sigue funcionando en otros contextos
- ✅ **No hay inversores** → no hay deadline artificial

---

## 📝 COMMIT MESSAGE

```
🧨 WAVE 2042.0: SCORCHED EARTH — Legacy Purge

OPERATION EXECUTED: Total elimination of legacy simulator architecture.

KILLED:
- StageSimulator2.tsx (1105 líneas monolito, zonas hardcoded)
- SimulateView/ folder (legacy container)
- StageViewDual.tsx + .css (estructura rígida, CSS básico)
- stage3d/ folder (Euler angles gimbal lock, materiales grises)
- layoutGenerator3D.ts (normalizer divergente)

SURVIVORS:
- controls/ (TheCommander, GroupsPanel, Sections — intactos)
- engine/, widgets/ (preparados para expansión)

BUILD STATUS: 🔴 BROKEN (expected until Phase 1)

NEXT: WAVE 2042.1 (Phase 0 — Canonical Zone Unification)

Hyperion se construye sobre tierra limpia, no sobre ruinas.
```

---

**FIN DEL REPORTE — EL TERRENO ESTÁ LIMPIO ☀️**

*"De las cenizas del legacy, nace la luz verdadera."*
