Auditoría Forense WAVE 4872 — The Ghost in the JSON
Veredicto
El bug no está en la serialización IPC ni en fs.writeFileSync. Es una regresión exacta del antipatrón que WAVE 4830 erradicó, reintroducida 60 líneas más abajo del propio comentario que advierte sobre ella.

Cadena de muerte (forense paso a paso)
Vector 1 — Deep Merge Drop: DESCARTADO
@/c:/Users/Raulacate/Desktop/Proyectos programacion/LuxSync/electron-app/src/core/orchestrator/IPCHandlers.ts:1264 hace deep clone y luego escritura plana:

Proyectos
const payload = JSON.parse(JSON.stringify(fixture)) as Record<string, any>
Proyectos
fs.writeFileSync(filePath, JSON.stringify(payload, null, 2), 'utf-8')
No hay merge con el JSON en disco. Lo que llegue al handler se escribe íntegro. Si el payload trajera el array vacío o ausente, el JSON reflejaría exactamente eso. El handler es inocente.

Vector 2 — La Resurrección por V1/V2 (Channel Rack ↔ Forge cells): EL CULPABLE
@c:/Users/Raulacate/Desktop/Proyectos programacion/LuxSync/electron-app/src/components/views/ForgeView/FixtureForgeEmbedded.tsx:999-1008

typescript
// WAVE 4830 — THE FRONTEND LIAR FIX:
// currentChannels (Channel Rack) es la única fuente de verdad para channels[].
// syncGraphOutputsWithChannels actualiza los nodos output_dmx del grafo para que
// el JSON persistido refleje el estado del Rack — pero NO se re-deriva channels
// desde el grafo. Re-derivar via toChannels(graphSnapshot) restauraba ignitionDeps
// ya eliminadas porque los nodos del canvas tienen config stale hasta el próximo
// loadGraph. El grafo es solo para la topología visual — el Rack manda en DMX.
Y a 59 líneas de distancia, el mismo antipatrón resucitado para el camino con células Aether:

Proyectos
// ── WAVE 4732-C: Si hay células Aether definidas, el compilador
//    genera el nodeGraph sobreescribiendo cualquier grafo previo.
//    Las células son la fuente de verdad cuando existen.
if (forgeState.cells.length > 0) {
  const compileResult = compileForgeState(forgeState)
  if (compileResult.ok) {
    builtFixture.nodeGraph = compileResult.fixture.nodeGraph
    // Propagar canales resueltos (ignitionDeps con targetChannelIndex)
    builtFixture.channels = compileResult.fixture.channels as FixtureChannel[]
La línea 1068 toma compileResult.fixture.channels derivado de forgeState.channels.map(ch => resolveChannelDeps(ch, state.channels)) (@c:/Users/Raulacate/Desktop/Proyectos programacion/LuxSync/electron-app/src/core/forge/compileForgeState.ts:393-395) y sobrescribe los channels Rack-truthed que se habían colocado limpios en @c:/Users/Raulacate/Desktop/Proyectos programacion/LuxSync/electron-app/src/components/views/ForgeView/FixtureForgeEmbedded.tsx:1018 (channels: syncedChannels).

Esto provoca dos modos de resurrección:

Carrera del sync V1→V2: el useEffect en @c:/Users/Raulacate/Desktop/Proyectos programacion/LuxSync/electron-app/src/components/views/ForgeView/FixtureForgeEmbedded.tsx:817-860 propaga fixture.channels → forgeState vía CHANNEL_REPLACE. Si handleSave corre en el mismo flush que el setFixture de la eliminación (clic rápido, onBlur + Save), forgeState.channels[idx] aún tiene la dep vieja → compileResult la re-emite → línea 1068 la resucita en builtFixture.channels → IPC la persiste fielmente.
Hidratación inicial sin replicación: si el fixture se cargó de disco con deps presentes, y posteriormente el usuario las elimina solo desde el Rack V1, el camino correcto depende 100% de que ese useEffect haya corrido. Cualquier dependencia React que invalide su ejecución bloquea la sincronización y deja forgeState zombi.
Vector 3 — Inmutabilidad ciega: PARCIAL (ruido, no resurrección)
@c:/Users/Raulacate/Desktop/Proyectos programacion/LuxSync/electron-app/src/core/forge/forgeBuilderState.ts:273-282 deja ignitionDeps: [] cuando se vacía:

Proyectos
case 'IGNITION_REMOVE': {
  const ch = state.channels[action.idx]
  if (!ch || !ch.ignitionDeps) return state
  const deps = ch.ignitionDeps.filter((_, i) => i !== action.depIdx)
  return {
    ...state,
    channels: patchChannel(state.channels, action.idx, { ignitionDeps: deps }),
    dirty:    true,
  }
}
Asimétrico respecto al handler V1 @c:/Users/Raulacate/Desktop/Proyectos programacion/LuxSync/electron-app/src/components/views/ForgeView/FixtureForgeEmbedded.tsx:953-958 que sí elimina la clave ({ ignitionDeps: _omit, ...rest }). Esta diferencia hace que el camino V2 deje "ignitionDeps": [] en el JSON, lo cual no resucita datos pero ensucia la salida y rompe la igualdad estructural (sameDeps en 833-842 sigue dando true porque ambos length === 0, así que tampoco redispatch).

El parche estructural mínimo
Una sola línea: eliminar (o blindar) la sobrescritura de builtFixture.channels en el bloque cells, manteniendo solo la propagación del nodeGraph.

Proyectos
if (forgeState.cells.length > 0) {
  const compileResult = compileForgeState(forgeState)
  if (compileResult.ok) {
    builtFixture.nodeGraph = compileResult.fixture.nodeGraph
    // Propagar canales resueltos (ignitionDeps con targetChannelIndex)
    builtFixture.channels = compileResult.fixture.channels as FixtureChannel[]
Reescrito (WAVE 4872):

typescript
if (forgeState.cells.length > 0) {
  const compileResult = compileForgeState(forgeState)
  if (compileResult.ok) {
    // WAVE 4872 — GHOST IN THE JSON:
    // Igual que WAVE 4830 selló el path sin células, sellamos también el
    // path con células: NO sobrescribir builtFixture.channels desde
    // compileResult. forgeState puede estar 1 tick desincronizado respecto
    // al React state (Channel Rack) y reintroduce ignitionDeps eliminadas.
    // El Rack manda en channels[]; las células solo aportan nodeGraph.
    builtFixture.nodeGraph = compileResult.fixture.nodeGraph
    if (compileResult.warnings.length > 0) {
      console.warn('[Forge 4732-C] Compile warnings:', compileResult.warnings)
    }
  } else { ... }
}
Si se necesita conservar la resolución de targetChannelIndex
resolveChannelDeps (@c:/Users/Raulacate/Desktop/Proyectos programacion/LuxSync/electron-app/src/core/forge/compileForgeState.ts:184-203) puede exportarse y aplicarse sobre builtFixture.channels (Rack-truthed) en lugar de descartarlos:

typescript
builtFixture.channels = builtFixture.channels.map(ch =>
  resolveChannelDeps(ch, builtFixture.channels)
)
Esto enriquece sin sustituir: si el Rack dice "esta dep ya no existe", se queda eliminada; si dice "existe pero sin targetChannelIndex", se resuelve.

Cleanup defensivo recomendado (Vector 3)
En @c:/Users/Raulacate/Desktop/Proyectos programacion/LuxSync/electron-app/src/core/forge/forgeBuilderState.ts:273-282, alinear la simetría con el handler V1 para no dejar ignitionDeps: [] colgando en el reducer V2:

typescript
case 'IGNITION_REMOVE': {
  const ch = state.channels[action.idx]
  if (!ch || !ch.ignitionDeps) return state
  const deps = ch.ignitionDeps.filter((_, i) => i !== action.depIdx)
  const patched = deps.length === 0
    ? (() => { const { ignitionDeps: _omit, ...rest } = ch; return rest })()
    : { ...ch, ignitionDeps: deps }
  return {
    ...state,
    channels: state.channels.map((c, i) => i === action.idx ? patched : c),
    dirty: true,
  }
}
Mismo trato para CELL_DELETE si llegase a deber limpiar referencias de canales — pero el reducer actual @c:/Users/Raulacate/Desktop/Proyectos programacion/LuxSync/electron-app/src/core/forge/forgeBuilderState.ts:325-329 solo filtra state.cells, no toca channels, lo cual es correcto: borrar una celda no debe borrar sus canales.

Resumen ejecutivo
Vector	Estado	Localización
1 — Deep Merge Drop	Descartado	IPC handler escribe plano
2 — Resurrección V1↔V2	Confirmado (root cause)	FixtureForgeEmbedded.tsx:1068
3 — Inmutabilidad reducer V2	Ruido secundario	forgeBuilderState.ts:273-282
El fantasma vive en una sola línea: builtFixture.channels = compileResult.fixture.channels (línea 1068). Eliminarla cierra la grieta por la que reaparecen las dependencias eliminadas. El propio comentario WAVE 4830 a 60 líneas arriba describe el patrón con quirúrgica precisión — solo faltaba aplicarlo también al camino con células.